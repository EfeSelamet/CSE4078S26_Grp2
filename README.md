# CSE4078S26_Grp2
NLP project

The code is juptier notebook code.

To run the code you need to download the models.
It may or may not download it automatically.
If it doesn't download it automatically you can downlaod it from hugging face.

First cell is to split the data into training, validation and testing.
Rest is testing and measuring the models.
The results are saved in to csv files that the code generates.
